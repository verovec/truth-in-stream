# Copy to .env (gitignored) and fill in. docker-compose interpolates these
# into the service environments.

# ElevenLabs API key for Scribe v2 speech-to-text.
TRANSCRIPTION_API_KEY=

# Voyage AI API key for embeddings (used by `make ingest`).
EMBEDDING_API_KEY=
