// The auth slot is empty unless an intercepting route fills it. default.tsx is
// what renders on a full load of any route that is not the intercepted /login.
export default function Default() {
  return null;
}
