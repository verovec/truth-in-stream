name: _terraform
on:
  workflow_call:
    inputs:
      path: { type: string, required: true }
      apply: { type: boolean, default: false }
    secrets:
      AWS_ROLE_ARN: { required: false }
jobs:
  terraform:
    runs-on: ubuntu-latest
    permissions: { id-token: write, contents: read }
    defaults: { run: { working-directory: "${{ inputs.path }}" } }
    env:
      AWS_ROLE_ARN: ${{ secrets.AWS_ROLE_ARN }}
    steps:
      - uses: actions/checkout@v4
      - uses: hashicorp/setup-terraform@v3
      - run: terraform fmt -check -recursive

      # No credentials (e.g. fork PRs): validate offline, no backend access.
      - if: env.AWS_ROLE_ARN == ''
        run: |
          terraform init -backend=false -input=false
          terraform validate

      # With credentials: real backend, then validate and plan.
      - if: env.AWS_ROLE_ARN != ''
        uses: aws-actions/configure-aws-credentials@e7f100cf4c008499ea8adda475de1042d6975c7b # v6
        with: { role-to-assume: "${{ env.AWS_ROLE_ARN }}", aws-region: eu-west-3 }
      - if: env.AWS_ROLE_ARN != ''
        run: |
          terraform init -input=false
          terraform validate
          terraform plan -input=false -out=tfplan

      # Fail if the apply role is missing any IAM permission the plan needs. The
      # apply role cannot grant itself new permissions, so this avoids a
      # half-applied change: the guard names the missing actions and the one
      # manual apply to run with elevated credentials. It runs on every plan with
      # credentials (PR and main), so a missing permission is caught before merge,
      # not only at the apply step.
      - if: env.AWS_ROLE_ARN != ''
        env:
          ENV_DIR: ${{ inputs.path }}
        run: |
          terraform show -json tfplan > tfplan.json
          bash "$GITHUB_WORKSPACE/scripts/iam-apply-guard.sh" tfplan.json "$AWS_ROLE_ARN" "$ENV_DIR"

      # Apply exactly the plan produced above (main only).
      - if: ${{ inputs.apply && env.AWS_ROLE_ARN != '' }}
        run: terraform apply -input=false tfplan
